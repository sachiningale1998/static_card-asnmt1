
import React from 'react';

const JoinUS = (props)=>{

    return (
        <div id='buttondiv'>
            <button id='btnJoin' > {props.heading} </button>
        </div>
    );

}

export default JoinUS ;